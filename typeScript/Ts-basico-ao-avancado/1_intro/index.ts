const firstName = "Matheus"
const anotherName = 1
const x = true

function greeting(name :string) {
    console.log(name)
}

greeting(firstName)
// greeting(anotherName)
// greeting(x)

export default 1